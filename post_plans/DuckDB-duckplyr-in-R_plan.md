# Plan: DuckDB-duckplyr-in-R

## Frontmatter

| Field | Value |
|---|---|
| title | DuckDB + duckplyr in R: Query 100M Rows Faster Than pandas on a Laptop |
| slug | DuckDB-duckplyr-in-R |
| description | DuckDB executes analytical SQL directly on files — no server needed. duckplyr lets you query DuckDB with dplyr syntax. Learn to query Parquet, CSV, and Arrow files from R with benchmarks. |
| keywords | duckplyr, duckplyr R, DuckDB dplyr, duckplyr tutorial, query parquet R, large data R, duckplyr vs dplyr |
| auto_link_terms | duckplyr\|duckplyr in R\|DuckDB dplyr\|duckplyr package\|df_from_parquet\|df_from_csv |
| auto_link_case_sensitive | true |
| mathjax | false |
| webr | false |
| date | 2026-04-06 |
| curriculum_id | MOD4 |
| post_type | FR |
| fr_parent | DuckDB-in-R.html |

## Breadcrumb (auto-generated by build.py)

`Home > Data Wrangling > DuckDB + duckplyr in R`

## Lead

duckplyr is a drop-in replacement for dplyr that runs your existing dplyr code on DuckDB's analytical engine — no SQL needed, no data loaded into memory until you ask for it.

## Introduction

Hook: you already know dplyr, but it loads everything into RAM. duckplyr gives you the same verbs — filter(), mutate(), summarise() — backed by DuckDB's columnar engine. Explain what duckplyr is, how it differs from raw DuckDB SQL, and what the reader will learn. Note: code runs in local R/RStudio (not in-browser) because duckplyr requires system dependencies.

## Core Sections

### H2 1: What is duckplyr and how does it differ from dplyr?
- Theory: drop-in replacement, lazy materialization, automatic fallback
- Code block 1: library(duckplyr) overrides dplyr, show same code working
- Inline exercise: try filtering mtcars with duckplyr loaded — predict output

### H2 2: How do you query CSV and Parquet files without loading them into memory?
- Theory: df_from_csv(), df_from_parquet(), lazy evaluation
- Code block 2: df_from_csv() on a local file
- Code block 3: df_from_parquet() on a local file
- Inline exercise: read a CSV with df_from_csv() and filter one column

### H2 3: How does duckplyr handle operations it cannot translate?
- Theory: fallback mechanism, which ops are supported, which aren't
- Code block 4: supported operation (group_by + summarise)
- Code block 5: unsupported operation triggers fallback with message
- Inline exercise: predict whether a given dplyr chain falls back

### H2 4: When should you use duckplyr vs raw DuckDB SQL?
- Theory: duckplyr for dplyr users, raw SQL for complex analytics, window functions
- Code block 6: same query in duckplyr vs dbGetQuery() side by side
- Inline exercise: rewrite a duckplyr chain as raw SQL

### H2 5: How fast is duckplyr compared to dplyr and data.table?
- Theory: columnar engine, vectorized execution, benchmark methodology
- Code block 7: microbenchmark comparing dplyr vs duckplyr vs data.table on aggregation
- Inline exercise: predict which approach wins for a filter-only query

## Tail Sections

### Common Mistakes (3)
1. Forgetting library(duckplyr) overrides dplyr — existing code silently changes engine
2. Assuming all dplyr verbs are translated — some fall back silently
3. Calling collect() too early — defeats lazy evaluation

### Practice Exercises (Capstone, 2)
1. Medium: read a CSV, filter, group_by, summarise, collect — chain from file to result
2. Hard: compare duckplyr vs dplyr timing on a generated 1M-row dataset

### Summary
Table: duckplyr vs dplyr vs raw DuckDB — when to use each.

### FAQ (3)
1. Does duckplyr work with all dplyr verbs? (narrow — coverage)
2. Can I use duckplyr with remote Parquet files on S3? (narrow — remote access)
3. Should I switch all my scripts to duckplyr? (narrow — migration advice)

### References (5)
1. duckplyr.tidyverse.org — official docs
2. DuckDB blog — duckplyr announcement
3. Appsilon — dplyr vs DuckDB benchmarks
4. DuckDB R client docs
5. Wickham — R for Data Science, dplyr chapter

### What's Next (2)
- DBI in R (DBI-in-R.html) — database connections
- DuckDB in R (DuckDB-in-R.html) — parent post

## Diagrams
None (FR post — skipping)

## Code Block Master List

| # | Demonstrates | Libs | Vars introduced | Vars used |
|---|---|---|---|---|
| 1 | duckplyr drop-in | duckplyr | mtcars_result | — |
| 2 | df_from_csv() | — | csv_df | — |
| 3 | df_from_parquet() | — | pq_df | — |
| 4 | Supported group_by+summarise | — | summary_result | — |
| 5 | Fallback demo | — | fallback_result | — |
| 6 | duckplyr vs SQL side-by-side | DBI | sql_result | — |
| 7 | Benchmark comparison | microbenchmark | bench_result | — |
