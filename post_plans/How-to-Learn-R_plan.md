# Plan: Learn R in 12 Months — Week-by-Week Roadmap

## A. Frontmatter

| Field | Value |
|---|---|
| title | Learn R in 12 Months: A Week-by-Week Roadmap With No Wasted Time |
| slug | How-to-Learn-R |
| description | A 12-month week-by-week R learning roadmap: data wrangling, stats, visualization, modeling, and packages — with milestones and projects at each stage. |
| keywords | how to learn R, learn R programming, R roadmap, R study plan, R programming for beginners, R data science path, R learning milestones |
| auto_link_terms | how to learn R\|learn R programming\|R learning roadmap\|R study plan\|R learning path\|12 month R roadmap\|R programming roadmap |
| auto_link_case_sensitive | false |
| mathjax | false |
| webr | true |
| date | 2026-04-13 |
| curriculum_id | CAR2 |
| post_type | FR |

No sidebar fields (FR type). No `fr_parent` (fr_core is null in curriculum).

## B. Breadcrumb
Auto-generated by build.py. Do not hand-write.

## C. Outline

### Lead paragraph
"Most people who try to learn R quit in the first month because they follow a plan that is either too vague ('just practice') or too ambitious ('become an expert in 30 days'). This 12-month roadmap gives you a specific weekly schedule, four milestone gates so you know when to move on, and an honest list of things you should skip."

### First H2: "What does the 12-month R roadmap look like at a glance?"

**Opening prose (~90 words):** Describe the four three-month phases — foundations, wrangling/visualisation, statistics/modelling, specialisation. Explain that each phase ends at a milestone gate with a project you must finish before moving on. Close with a "taste of R" invitation to run the first code block.

**Block 1 (payoff, in first H2):** `summary(mtcars$mpg)` — one-line statistical summary to show how much R gives you with almost no code. Output inline with `#>`. Interpretation: by end of month 2 this will feel natural.

**Inline exercise:** ask reader to summarise `mtcars$hp` with `summary()`. Reveal solution.

### H2 2: "Months 1-3: What should you learn in the foundations phase?"

Cover:
- Weeks 1-2: install R and RStudio, arithmetic, assignment (`<-`), basic objects
- Weeks 3-4: vectors, lists, data frames, indexing
- Weeks 5-8: functions, control flow, file I/O basics, `read.csv()`
- Weeks 9-12: exploratory base-R analysis of one real dataset (milestone project)

Block 2: vector + mean/sd example, showing what a week-4 reader can write. Variables: `ages`.
Callout [NOTE]: skip advanced environments and OOP in month 1 — it's a classic trap.
Inline exercise: compute the mean of a small vector.

### H2 3: "Months 4-6: How do you master data wrangling and visualisation?"

Cover:
- Weeks 13-16: dplyr verbs (filter, select, mutate, summarise, group_by)
- Weeks 17-20: tidyr (pivot_longer/wider), joins, date/string handling
- Weeks 21-24: ggplot2 grammar, layering, themes, milestone project — an EDA report

Block 3: dplyr pipeline on mtcars grouped by cyl, shows group means. Uses `mtcars_df` from earlier state.
Block 4: ggplot2 scatter of mpg vs wt, colour by cyl.
Callout [TIP]: learn the native pipe `|>` first; magrittr `%>%` can come later.
Inline exercise: filter `mtcars` for `mpg > 25` and count rows.

### H2 4: "Months 7-9: Which statistics and modelling topics matter most?"

Cover:
- Weeks 25-28: descriptive stats, distributions, hypothesis tests
- Weeks 29-32: linear regression with `lm()`, diagnostics, `broom::tidy()`
- Weeks 33-36: GLMs, logistic regression, milestone project — model a real outcome

Block 5: `lm(mpg ~ wt + cyl, data = mtcars)` fit, summary.
Callout [KEY INSIGHT]: statistics fluency is what sets R apart from Python — do not skip this phase.
Inline exercise: fit a single-predictor `lm()` on mtcars.

### H2 5: "Months 10-12: How do you pick a specialisation?"

Cover:
- Decision flowchart (diagram)
- Tracks: ML (tidymodels), Shiny apps, Time series (fable/forecast), Bioinformatics, Finance
- Week-by-week week 37-48 split depends on track; week 49-52 is a capstone portfolio project

Block 6: quick `tidymodels` placeholder — train a simple logistic model. Since tidymodels is heavy, use base `glm()` for WebR compatibility.
Diagram 2: specialisation decision flowchart.
Inline exercise: fit a `glm()` on mtcars (am ~ mpg) for a binary classifier.

### H2 6: "How do you know you're ready to move to the next phase?"

Concrete gate criteria for each of the 4 phases — a checklist you can self-assess.
Block 7: a small "gate test" — can you write this pipeline? Uses dplyr groupby+summarise.
Callout [WARNING]: do not skip gates because of impatience — weak foundations compound.
Inline exercise: self-assessment — run a short snippet and predict the output.

### H2 7: "What common mistakes waste beginners' time?"

Anti-patterns:
- Tutorial hopping without finishing one
- Learning every package at once
- Avoiding statistics because "it's hard"
- Copying code without reading output
- Starting Shiny before dplyr feels easy

Block 8: tiny example of a common bug — `mean(c(1, 2, NA))` returns NA unless you use `na.rm = TRUE`.
Callout [WARNING]: NA handling silently wrecks analyses.
Inline exercise: fix the NA bug by adding `na.rm = TRUE`.

### Tail sections

**Practice Exercises (2 capstone):**
1. Build a mini-EDA pipeline on `airquality` — filter, group, summarise, plot. Medium.
2. Fit and interpret a regression on `mtcars` with 2 predictors and report R-squared. Hard.

**Complete Example / Putting It All Together:**
End-to-end pipeline on `iris`: load → summarise by species → fit logistic-ish classifier (via `glm(Species=='setosa' ~ ...)`) → report accuracy. Represents the skills of a month-9 reader.

**Summary:**
Table — Phase | Months | Skills | Milestone Project
Diagram 1 (timeline) sits here as visual recap.

**References (5-8):**
1. Wickham & Grolemund — R for Data Science (2e). https://r4ds.hadley.nz/
2. Wickham — Advanced R (2e). https://adv-r.hadley.nz/
3. CRAN — Introduction to R manual. https://cran.r-project.org/doc/manuals/r-release/R-intro.html
4. Posit Cheatsheets. https://posit.co/resources/cheatsheets/
5. Tidyverse documentation. https://www.tidyverse.org/
6. ISLR — An Introduction to Statistical Learning. https://www.statlearning.com/
7. R-bloggers archive. https://www.r-bloggers.com/
8. Stack Overflow [r] tag. https://stackoverflow.com/questions/tagged/r

**Continue Learning (3):**
- R Data Types
- dplyr filter() and select()
- ggplot2 basics

## D. Diagrams

| # | Filename | Figure | Caption | Placed in H2 |
|---|---|---|---|---|
| 1 | How-to-Learn-R-phases-timeline.webp | Figure 1 | The 12-month roadmap split into four three-month phases with milestone gates. | Summary |
| 2 | How-to-Learn-R-specialisation-choice.webp | Figure 2 | A simple decision flow for picking a specialisation in months 10-12. | Months 10-12: How do you pick a specialisation? |

## E. Code block master list

| # | Demonstrates | Libs | Vars introduced | Vars used |
|---|---|---|---|---|
| 1 | Taste of R — one-line summary | — | — | — |
| 2 | Vectors + mean/sd (foundations) | — | ages | — |
| 3 | dplyr groupby pipeline | dplyr | mpg_by_cyl | — |
| 4 | ggplot2 scatter | ggplot2 | — | — |
| 5 | lm() regression | broom | fit | — |
| 6 | glm() binary classifier | — | glm_fit | — |
| 7 | Gate-test pipeline | — | gate_result | — |
| 8 | NA handling bug | — | — | — |

Inline exercise blocks use `ex_*` prefix and do not carry over.
