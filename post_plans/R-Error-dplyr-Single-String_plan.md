# Plan: dplyr group_by Error: 'must return a single string' — The .data[[]] Fix

## A. Frontmatter

| Field | Value |
|---|---|
| `title` | dplyr group_by Error: 'must return a single string' — The .data[[]] Fix |
| `slug` | R-Error-dplyr-Single-String |
| `description` | The dplyr 'must return a single string' error appears when you pass a column name programmatically to group_by(). Fix it with .data[[col]] or {{ col }}. |
| `keywords` | dplyr group_by error, must return a single string, dplyr tidy evaluation, .data pronoun, embrace operator, dplyr programming, dplyr column variable |
| `auto_link_terms` | dplyr group_by error\|must return a single string\|.data pronoun\|embrace operator\|dplyr tidy evaluation |
| `auto_link_case_sensitive` | false |
| `mathjax` | false |
| `webr` | true |
| `date` | 2026-04-13 |
| `curriculum_id` | ERR11 |
| `post_type` | FR |
| `fr_parent` | R-Common-Errors.html |

## B. Breadcrumb

Home > Learn R > Common Errors > dplyr group_by Error: 'must return a single string' (auto-generated by build.py)

## C. Section Outline

**Lead sentence:** The dplyr "must return a single string" error fires when you hand `group_by()` a column name stored in a variable. dplyr's tidy evaluation reads bare names as columns and strings as literal values — to group by a stored column name, wrap the variable in `.data[[col]]` (for strings) or `{{ col }}` (for bare names inside functions).

**First H2 opening plan (≤80 words, between heading and payoff code):**
"You hit this error the moment you try to be clever about grouping. Instead of writing `group_by(cyl)`, you store the column name in a variable and pass it in — and dplyr complains with a confusing message about strings and lengths. Below is the smallest reproduction. Run it once, then we'll unpack why dplyr disagrees with what you meant."

### Core H2 sections

**1. ## What does 'must return a single string' mean in dplyr?**
- Theory: This is a tidy-evaluation error, not a summarise-length error. dplyr treats bare names and strings differently.
- Code block 1 (payoff): `library(dplyr); my_col <- "cyl"; mtcars |> group_by(my_col) |> summarise(n = n())` → error output
- NOTE callout: error message exact wording varies by dplyr version; cause is the same
- Inline exercise: store "gear" in ex_col and reproduce the error

**2. ## Why does dplyr treat my variable as a column name?**
- Theory: tidy evaluation explained plainly. dplyr looks at the symbol `my_col`, not its value.
- Code block 2: show `c("cyl", "gear")` variant producing same class of error
- KEY INSIGHT callout: "dplyr reads what you typed, not what your variable points to."
- Inline exercise: predict what happens when ex_col holds a valid column vs an invalid one

**3. ## How do you fix it with .data[[col]]?**
- Theory: `.data` is a tidy-eval pronoun, a stand-in for the data frame; `[[ ]]` indexes it by string.
- Code block 3: `mtcars |> group_by(.data[[my_col]]) |> summarise(mean_mpg = mean(mpg))` → tibble output
- TIP callout: why `.data[[]]` is preferred over `!!sym(col)`
- Inline exercise: fix a broken pipeline by wrapping the string variable

**4. ## When should you use {{ col }} instead?**
- Theory: embrace operator for user-facing functions that accept a bare column name
- Code block 4: `group_summary <- function(df, group_col) { df |> group_by({{ group_col }}) |> summarise(mean_mpg = mean(mpg), .groups = "drop") }; group_summary(mtcars, cyl)`
- NOTE callout: `{{ }}` requires a bare name from the caller; use `.data[[]]` if they pass a string
- Inline exercise: write ex_summary() that accepts a bare column and returns medians

**5. ## How do .data[[]] and {{ }} compare?**
- Comparison table: input form → pattern → use case
- Diagram 1: decision flowchart (`R-Error-dplyr-Single-String-fix-decision.webp`)
- Code block 5: `across(all_of(c("cyl","gear")))` for vector-of-strings case
- Inline exercise: regroup by c("am","gear")

**6. ## What other dplyr verbs need .data[[]]?**
- Theory: same pattern works in filter, arrange, mutate, summarise, select
- Code block 6: one-block demo chaining filter/arrange/mutate with `.data[[my_col]]`
- WARNING callout: don't mix `my_col` and `.data[[my_col]]` in the same expression
- Inline exercise: filter + arrange using ex_col

### Tail sections

**## Practice Exercises** (2 capstones):
1. *Medium:* Write a function `summarise_by_col(df, col_string, metric_col)` that groups by a string column name and returns mean of another column. Test on mtcars with `"cyl"` and `"mpg"`.
2. *Hard:* Write `flex_group_summary()` that accepts EITHER a bare name OR a string (hint: use `rlang::ensym()` or a branching approach). Show it works both ways.

**## Complete Example**
- Build a config-driven summary function that takes a vector of group columns (as strings) plus a metric column (as string) and produces the grouped summary. Apply to mtcars.

**## Summary**
- Table: pattern → when to use → example

**## References** (5):
1. dplyr programming vignette — https://dplyr.tidyverse.org/articles/programming.html
2. rlang `.data` pronoun — https://rlang.r-lib.org/reference/dot-data.html
3. tidyselect `all_of()` / `any_of()` — https://tidyselect.r-lib.org/reference/all_of.html
4. Hadley Wickham — *Advanced R* 2e, Chapter 19 (Quasiquotation) — https://adv-r.hadley.nz/quasiquotation.html
5. tidyverse blog — "dplyr 0.7.0" (introduction of tidy evaluation) — https://www.tidyverse.org/blog/2017/06/dplyr-0-7-0/

**## Continue Learning**
1. 50 R Errors Decoded (parent, R-Common-Errors.html)
2. dplyr group_by() basics (if a C post exists; otherwise dplyr intro)
3. Writing functions in R

## D. Diagrams

| # | Filename | Figure N | Caption | Placed in H2 section |
|---|---|---|---|---|
| 1 | R-Error-dplyr-Single-String-fix-decision.webp | Figure 1 | Choosing between `.data[[col]]`, `{{ col }}`, and `across(all_of())` based on how the column name is supplied. | How do .data[[]] and {{ }} compare? |

## E. Code Block Master List

| Block # | Demonstrates | Libs | Vars introduced | Vars used (from prior) |
|---|---|---|---|---|
| 1 | Reproduce the error (payoff) | `dplyr` | `my_col` | — |
| 2 | Vector-of-strings variant | — | `cols` | — |
| 3 | Fix with .data[[col]] | — | — | `my_col` |
| 4 | Function with {{ col }} | — | `group_summary` | — |
| 5 | across(all_of()) for multi-col | — | `group_cols` | — |
| 6 | .data[[]] in filter/arrange/mutate | — | — | `my_col` |
| C1 (capstone 1) | summarise_by_col() | — | `summarise_by_col`, `my_result` | — |
| C2 (capstone 2) | flex_group_summary() | — | `flex_group_summary` | — |
| CE (complete example) | Config-driven summary pipeline | — | `config_cols`, `metric_col`, `final_summary` | — |
