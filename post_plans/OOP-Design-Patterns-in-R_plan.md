# Plan: OOP Design Patterns in R: Factory, Strategy & Observer in R6

## A. Frontmatter

| Field | Value |
|---|---|
| title | OOP Design Patterns in R: Factory, Strategy & Observer in R6 |
| slug | OOP-Design-Patterns-in-R |
| description | Implement Factory, Strategy, Observer, Singleton & Builder design patterns in R using R6. Runnable examples, when to use each, and practical R6 idioms. |
| keywords | R6 design patterns, factory pattern R, strategy pattern R, observer pattern R, singleton pattern R, builder pattern R, OOP patterns R, R6 class examples |
| auto_link_terms | OOP design patterns in R\|design patterns in R6\|factory pattern in R\|strategy pattern in R\|observer pattern in R\|singleton pattern in R\|builder pattern in R |
| auto_link_case_sensitive | false |
| mathjax | false |
| webr | true |
| date | 2026-04-13 |
| curriculum_id | FR-oop-3 |
| post_type | FR |
| fr_parent | R6-Classes-in-R.html |

## B. Breadcrumb

`Home > Learn R > OOP > OOP Design Patterns in R: Factory, Strategy & Observer in R6` — auto-generated by build.py.

## C. Section Outline

**Lead (`<p class="lead">`):** Design patterns are reusable solutions to recurring OOP problems. R6's mutable reference classes make the classic Gang-of-Four patterns — Factory, Strategy, Observer, Singleton and Builder — natural and useful in R.

**First H2 opening (≤80 words):** A factory is a function whose only job is to decide which class to build and hand you back an instance you can use without caring about the choice it made. It keeps the selection rule in one place and lets the rest of your code treat the result uniformly. The classic R use case: a single `read()` method that silently routes CSV, JSON and Excel files to the right reader.

### Core sections

1. **How does the Factory Pattern work in R6?**
   - Define three readers with a shared `read()` interface
   - `create_reader()` factory switches on file extension
   - Code block: three R6 classes + factory + usage
   - KEY INSIGHT: callers depend on the interface, not the concrete class
   - Try it: add an `RDSReader` to the factory

2. **How does the Strategy Pattern swap behavior at runtime?**
   - Context class holds a strategy object and delegates to it
   - Three interchangeable scorers (mean / median / trimmed mean)
   - Code block: Strategy classes + Context + swap mid-run
   - TIP: strategies can be plain functions if state isn't needed
   - Try it: add a `MaxStrategy`

3. **How does the Observer Pattern notify listeners of state changes?**
   - Subject maintains a list of observers and emits `notify()` on change
   - Two observers: logger and threshold alerter
   - Code block: Subject + two observers + subscribe/unsubscribe
   - WARNING: private lists of R6 refs are aliased — never copy, always modify in place
   - Try it: add an `AverageObserver`

4. **When should you reach for Singleton or Builder patterns?**
   - Singleton: one shared config object via closure trick
   - Builder: fluent chainable step-by-step object construction
   - Two compact code blocks
   - NOTE: R's package namespaces already give you a de-facto singleton
   - Try it: add a `set_port()` method to the config singleton

5. **How do you choose the right pattern for your problem?**
   - Decision guide table: symptom → pattern
   - Code block: picking Strategy over a giant `if/else` block
   - KEY INSIGHT: patterns are vocabulary for explaining design, not mandatory scaffolding

### Tail sections

6. **Practice Exercises** — 2 capstone exercises:
   - Ex 1 (medium): Build a Strategy-based `Discount` system with three strategies and a `Cart` context
   - Ex 2 (hard): Combine Factory + Observer — a `Notifier` factory that returns email/sms/slack notifiers and a central `AlertHub` that broadcasts to all of them

7. **Complete Example** — Mini pipeline that uses Factory (pick reader) + Strategy (pick scorer) + Observer (log results) together on `mtcars`.

8. **Summary** — 5-row table: pattern / intent / when to reach for it.

9. **References** — 6 sources: Advanced R Ch.14, R6 CRAN docs, R6P package, Gamma et al. GoF book, Refactoring.Guru, tidyverse R6 vignette.

10. **Continue Learning** — 3 linked posts: R6-Classes-in-R, OOP-in-R, S4-Classes-in-R.

## D. Diagrams

| # | Filename | Figure | Caption | Placed in H2 |
|---|---|---|---|---|
| 1 | OOP-Design-Patterns-in-R-factory-flow.webp | Figure 1 | How a factory routes one call to the right reader class. | How does the Factory Pattern work in R6? |
| 2 | OOP-Design-Patterns-in-R-observer-seq.webp | Figure 2 | Subject notifies every subscribed observer when state changes. | How does the Observer Pattern notify listeners of state changes? |
| 3 | OOP-Design-Patterns-in-R-pattern-map.webp | Figure 3 | The five R6 patterns grouped by purpose. | How do you choose the right pattern for your problem? |

## E. Code block master list

| # | Demonstrates | Libs | Vars introduced | Vars used |
|---|---|---|---|---|
| 1 | Factory: 3 readers + create_reader | R6 | CSVReader, JSONReader, ExcelReader, create_reader, reader1, reader2 | — |
| 2 | Try it (Factory): add RDSReader (scaffold) | — | ex_create_reader | — |
| 3 | Try it (Factory): solution | — | ex_RDSReader | ex_create_reader |
| 4 | Strategy: scorer strategies + Context | — | MeanScore, MedianScore, TrimmedScore, Scorer, sc | — |
| 5 | Try it (Strategy): MaxStrategy (scaffold) | — | ex_MaxStrategy | Scorer |
| 6 | Try it (Strategy): solution | — | ex_MaxStrategy, ex_sc | Scorer |
| 7 | Observer: Subject + Logger + Alerter | — | TempSensor, LoggerObs, AlertObs, sensor, log1, alert1 | — |
| 8 | Try it (Observer): AverageObserver (scaffold) | — | ex_AverageObs | TempSensor |
| 9 | Try it (Observer): solution | — | ex_AverageObs, ex_sensor | TempSensor |
| 10 | Singleton: config via closure | — | get_config, cfg1, cfg2 | — |
| 11 | Builder: fluent ReportBuilder | — | ReportBuilder, report | — |
| 12 | Try it (Singleton/Builder): add set_port (scaffold) | — | — | get_config |
| 13 | Try it (Singleton/Builder): solution | — | — | get_config |
| 14 | Decision: Strategy replaces nested if/else | — | classify_v1, classify_v2, SlopeStrategy, FlatStrategy, classifier | — |
| 15 | Try it (Decision): pick pattern for problem (scaffold) | — | — | — |
| 16 | Try it (Decision): solution | — | — | — |
| 17 | Complete example: Factory + Strategy + Observer pipeline | — | Reader, pipe_sensor, pipe_logger, pipe_scorer, result | — |

Rules respected: libraries only in block 1; every "used" var appears in an earlier block's "introduced" column.
