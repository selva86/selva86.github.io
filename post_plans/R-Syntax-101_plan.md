# Plan: R Syntax 101

## Frontmatter

| Field | Value |
|---|---|
| title | R Syntax 101: Write Your First Working Script in 10 Minutes |
| slug | R-Syntax-101 |
| description | Learn R's core syntax: arithmetic operators, variable assignment with <-, comments, and how to write and run your first script — with every line explained. |
| keywords | R syntax, R arithmetic operators, R assignment operator, R comments, R script, learn R basics, R first script, operator precedence R, R variables, R for beginners |
| auto_link_terms | R syntax\|basic R syntax\|R arithmetic operators\|assignment operator in R\|R comments\|writing R scripts |
| auto_link_case_sensitive | false |
| mathjax | false |
| webr | true |
| date | 2026-04-11 |
| curriculum_id | 1.1.4 |
| post_type | C |
| sidebar_section | Learn R |
| sidebar_title | R Syntax 101 |
| sidebar_order | 5 |

Breadcrumb: Home > Learn R > R Language from Scratch > R Syntax 101 (auto-generated by build.py)

## Lead
R's syntax is the small set of rules that lets you do arithmetic, store values with `<-`, write `#` comments, and save it all as a `.R` script. Master these four pieces and you can write real R code today.

## Introduction
- Hook: "If you can type 2 + 2 and read the answer, you already understand the shape of an R script."
- Everything advanced (models, dplyr, ggplot2) is built on four primitives: arithmetic, assignment, comments, scripts.
- Promise: each rule gets a runnable example.
- Figure 1 (mindmap) after intro paragraphs.
- Budget: ≤200 words before first code block.

## Core H2 sections (question form)

### 1. How do you do arithmetic in R?
Theory → Code block 1 (payoff) with all six operators. Then precedence code block (parens). Figure 2. Built-in math functions. Inline exercise: compound interest.

### 2. How do you assign values to variables in R?
`<-` intuition, name <- value, reassignment, naming rules. Figure 3. Callouts: Alt+- shortcut (TIP), snapshot semantics (KEY INSIGHT). Inline exercise: product of two vars.

### 3. How do you write comments in R?
Single-line `#`, inline comments, section headers. NOTE callout on Ctrl+Shift+C. Inline exercise: comment out one line.

### 4. How do you write and run your first R script?
What a `.R` file is. Three ways to run: Source button, `source()`, `Rscript`. Full temp converter script. Figure 4. TIP: header comment. Inline exercise: F→C direction.

### 5. What other operators will you use all the time?
Comparisons, logicals. WARNING callout: `=` vs `==` in `if()`. Inline exercise: range check.

## Common Mistakes (5)
1. `=` vs `==` in `if()`
2. Variable starts with a digit
3. Treating `#` as block comment
4. Overwriting `T`, `c`, `data`
5. `/` vs `%/%` (integer division)

## Practice Exercises (2 capstone)
1. BMI calculator (medium)
2. Tip + split calculator (harder, multi-step)

## Complete Example
Monthly budget summary script: variables, arithmetic, comments, printed result.

## Summary
Table: operator/concept → purpose → example.

## FAQ (5)
1. Why prefer `<-` over `=`? (narrow comparison)
2. Multiple statements per line?
3. Is R case-sensitive?
4. `/` vs `%/%` difference (single-operator narrow)
5. Clearing variables from memory (rm)

## References (5)
1. R Core Team — *An Introduction to R*
2. Wickham — *Advanced R*, Ch. 2
3. tidyverse style guide
4. CRAN R Language Definition §3
5. Paradis — *R for Beginners*

## What's Next?
1. R Data Types
2. R Vectors
3. Install R and RStudio

## Diagrams

| # | Filename | Figure | Caption | Placed in H2 |
|---|---|---|---|---|
| 1 | R-Syntax-101-syntax-mindmap.webp | 1 | The four building blocks of R syntax. | Introduction |
| 2 | R-Syntax-101-operator-precedence.webp | 2 | Operator precedence in R follows PEMDAS. | How do you do arithmetic in R? |
| 3 | R-Syntax-101-assignment-flow.webp | 3 | The `<-` operator sends a value into a name. | How do you assign values to variables in R? |
| 4 | R-Syntax-101-script-execution.webp | 4 | Three ways to run an R script top-to-bottom. | How do you write and run your first R script? |

## Code block master list

| # | Demonstrates | Vars introduced | Vars used |
|---|---|---|---|
| 1 | Six arithmetic operators | — | — |
| 2 | Operator precedence with parens | — | — |
| 3 | Built-in math functions | — | — |
| 4 | Assignment with `<-` | weight_kg, height_m, bmi | — |
| 5 | Reassignment | counter | — |
| 6 | Comment styles | — | — |
| 7 | Temp converter script | celsius, fahrenheit | — |
| 8 | Comparison operators | — | celsius |
| 9 | Logical combinations | — | celsius |
| 10 | BMI solution | my_weight_kg, my_height_m, my_bmi | — |
| 11 | Tip split solution | my_bill, my_tip_pct, my_people, my_total, my_share | — |
| 12 | Budget script | income, rent, groceries, transport, total_expenses, disposable, savings | — |

No `library()` — pure base R. WebR-safe.

## Callouts (~5-6 total)
- TIP: parentheses in arithmetic
- TIP: RStudio Alt+- shortcut
- KEY INSIGHT: assignment snapshot
- NOTE: Ctrl+Shift+C to toggle comments
- TIP: header comment at top of scripts
- WARNING: `=` vs `==` in `if()`

## Inline exercises
1. Arithmetic: compound interest
2. Assignment: product of two vars
3. Comments: comment out one line
4. Scripts: F→C direction
5. Operators: range check (10 < x < 20)
