# Plan: R Syntax 101

## Frontmatter

| Field | Value |
|---|---|
| title | R Syntax 101: Write Your First Working Script in 10 Minutes |
| slug | R-Syntax-101 |
| description | Learn R's core syntax: arithmetic operators, variable assignment with <-, comments, and how to write and run your first script — with every line explained. |
| keywords | R syntax, R arithmetic operators, R assignment operator, R comments, R script, learn R basics, R first script, operator precedence R, R variables, R for beginners |
| auto_link_terms | R syntax\|basic R syntax\|R arithmetic operators\|assignment operator in R\|R comments\|writing R scripts |
| auto_link_case_sensitive | false |
| mathjax | false |
| webr | true |
| date | 2026-04-11 |
| curriculum_id | 1.1.4 |
| post_type | C |
| sidebar_section | Learn R |
| sidebar_title | R Syntax 101 |
| sidebar_order | 5 |

Breadcrumb (auto-generated by build.py): Home > Learn R > R Language from Scratch > R Syntax 101

## Lead (≈40 words)
R's syntax is the small set of rules that lets you do arithmetic, store values with `<-`, write `#` comments, and save it all as a `.R` script. Master these four pieces and you can write real R code today.

## First H2 opening plan (≤80 words)
"Arithmetic is the gentlest way into R. Type an expression, press Enter, and R prints the answer — no `print()` call, no setup, no boilerplate. Every calculator key you've ever used has a one-character equivalent in R, plus two handy ones Excel doesn't give you by default." Then payoff code block: all six operators at once, each with `#>` output.

Time-to-first-code: Lead (~40 words) + H2 opening (~55 words) ≈ 95 words. Under budget.

## Core H2 sections (5)

### 1. How do you do arithmetic in R? (figure: operator-precedence after first block)
- Payoff code block: `+`, `-`, `*`, `/`, `^`, `%%` with outputs.
- Prose interpretation walking through each line.
- Second block: precedence with and without parentheses (`2 + 3 * 4` vs `(2 + 3) * 4`).
- Figure 1: operator-precedence.
- TIP callout: always reach for parentheses when mixing operators.
- Built-in math functions block (`sqrt`, `log`, `exp`, `abs`, `round`).
- Inline exercise: compound interest one-liner.

### 2. How do you assign values to variables in R? (figure: assignment-flow)
- Intuition: `x <- 5` reads "send 5 into x". Picture an arrow.
- Code block: create `weight_kg`, `height_m`, compute `bmi`.
- Prose interpretation showing the BMI result.
- Figure 2: assignment-flow.
- Naming rules code block (valid vs invalid names shown, with comments).
- TIP: Alt+- keyboard shortcut in RStudio.
- KEY INSIGHT: `<-` is a snapshot, not a link — reassigning `weight_kg` later leaves `bmi` unchanged.
- Reassignment code block using a `counter` variable.
- Inline exercise: product of two variables.

### 3. How do you add comments to R code?
- Explain `#`: everything after it on the line is ignored.
- Code block: full-line comments, inline comments, section divider comments.
- Prose interpretation.
- NOTE: Ctrl+Shift+C toggles comments on selected lines in RStudio.
- Inline exercise: comment out one line so the result changes.

### 4. How do you write and run your first R script? (figure: script-execution)
- What a `.R` file is: plain text, runs top-to-bottom.
- Code block: a 10-line temperature converter script (comments + assignments + arithmetic + `print()`).
- Figure 3: script-execution.
- Three ways to run: Source button, `source("file.R")`, `Rscript file.R` from terminal.
- TIP: start every script with a header comment.
- Inline exercise: extend the script to convert Fahrenheit → Celsius.

### 5. What other operators will you use all the time?
- Comparison operators: `<`, `>`, `<=`, `>=`, `==`, `!=` — return TRUE/FALSE.
- Code block: compare two numbers, demo `==` vs `=`.
- WARNING: using `=` inside `if()` is a silent bug in most languages but an outright error in R — explain why.
- Logical operators: `&`, `|`, `!`.
- Code block: combine comparisons to test a range.
- Inline exercise: check whether a number is between 10 and 20.

## Practice Exercises (2 capstone)

### Exercise 1: BMI calculator (medium)
Compute BMI from weight and height, categorize as "Under", "Normal", or "Over". Combines assignment, arithmetic, and a comparison chain. Uses `my_weight`, `my_height`, `my_bmi`.

### Exercise 2: Tip + split calculator (harder, multi-step)
Given a bill, tip %, and number of diners, compute total and per-person share. Combines assignment, multiplication, division, and formatted output with `round()`. Uses `my_bill`, `my_tip_pct`, `my_people`, `my_share`.

## Complete Example — Monthly Budget Script
End-to-end script showing every concept in one place: header comment, assignments, arithmetic, section comments, printed result with interpretation prose.

## Summary (figure: syntax-mindmap at top)
- Figure 4: syntax-mindmap — visual recap of the 4 pillars.
- Table: operator/concept → purpose → example.

## References (5)
1. R Core Team — *An Introduction to R*, §1.8 Arithmetic. [Link](https://cran.r-project.org/doc/manuals/r-release/R-intro.html#Simple-manipulations-numbers-and-vectors)
2. Wickham, H. — *Advanced R*, 2nd Ed., Chapter 2: Names and values. [Link](https://adv-r.hadley.nz/names-values.html)
3. tidyverse style guide — Syntax. [Link](https://style.tidyverse.org/syntax.html)
4. CRAN — *R Language Definition*, §3 Evaluation of expressions. [Link](https://cran.r-project.org/doc/manuals/r-release/R-lang.html#Evaluation-of-expressions)
5. Paradis, E. — *R for Beginners*. [Link](https://cran.r-project.org/doc/contrib/Paradis-rdebuts_en.pdf)

## What's Next? (2-3 related posts)
1. R Data Types — the numeric/integer/character/logical types that live inside your variables.
2. R Vectors — how one value becomes many, the real unit of R.
3. Install R and RStudio — if you haven't set up the environment yet.

## Diagrams

| # | Filename | Figure | Caption | Placed in H2 |
|---|---|---|---|---|
| 1 | R-Syntax-101-operator-precedence.webp | 1 | R evaluates operators in PEMDAS order — parentheses first, assignment last. | How do you do arithmetic in R? |
| 2 | R-Syntax-101-assignment-flow.webp | 2 | The `<-` operator sends a value into a name. | How do you assign values to variables in R? |
| 3 | R-Syntax-101-script-execution.webp | 3 | A script runs top-to-bottom, skipping comment lines. | How do you write and run your first R script? |
| 4 | R-Syntax-101-syntax-mindmap.webp | 4 | The four building blocks of R syntax in one view. | Summary |

No diagram appears above the first code block. Mindmap goes in Summary as a visual recap.

## Code block master list

| # | Demonstrates | Libs | Vars introduced | Vars used (from prior) |
|---|---|---|---|---|
| 1 | Six arithmetic operators (payoff) | — | — | — |
| 2 | Operator precedence with parens | — | — | — |
| 3 | Built-in math functions | — | — | — |
| 4 | Inline exercise: compound interest | — | `ex_principal`, `ex_rate`, `ex_years` | — |
| 5 | Assignment: weight, height, BMI | — | `weight_kg`, `height_m`, `bmi` | — |
| 6 | Naming rules (valid/invalid) | — | `sales_2026`, `mean_mpg` | — |
| 7 | Reassignment with counter | — | `counter` | — |
| 8 | Inline exercise: product | — | `ex_a`, `ex_b` | — |
| 9 | Comment styles | — | `radius` | — |
| 10 | Inline exercise: comment toggle | — | `ex_price`, `ex_tax` | — |
| 11 | Temperature converter script | — | `celsius`, `fahrenheit` | — |
| 12 | Inline exercise: F→C | — | `ex_f`, `ex_c` | — |
| 13 | Comparison operators | — | — | `celsius` |
| 14 | Logical combinations | — | — | `celsius` |
| 15 | Inline exercise: range check | — | `ex_score` | — |
| 16 | Capstone 1: BMI + category | — | `my_weight`, `my_height`, `my_bmi` | — |
| 17 | Capstone 2: Tip + split | — | `my_bill`, `my_tip_pct`, `my_people`, `my_total`, `my_share` | — |
| 18 | Complete Example: budget script | — | `income`, `rent`, `groceries`, `transport`, `total_expenses`, `savings` | — |

Pure base R — no `library()` calls. WebR-safe. Capstone exercises use `my_` prefix to avoid tutorial-state collision. Inline exercises use `ex_` prefix.

## Callouts (target ≈ 6 for ~2500 words)
1. TIP — parentheses are free, use them liberally (in arithmetic section)
2. TIP — Alt+- shortcut for `<-` in RStudio (in assignment section)
3. KEY INSIGHT — `<-` is a snapshot, not a link (in assignment section)
4. NOTE — Ctrl+Shift+C toggles comments in RStudio (in comments section)
5. TIP — header comment at the top of every script (in scripts section)
6. WARNING — `=` in `if()` is an error in R, not a silent bug (in operators section)

## Inline exercises (1 per core H2, 5 total)
1. Arithmetic — compound interest one-liner
2. Assignment — product of two variables
3. Comments — toggle one line via comment
4. Scripts — extend script for F→C direction
5. Operators — "between 10 and 20" range check
