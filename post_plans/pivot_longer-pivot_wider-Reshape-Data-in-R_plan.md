# Plan: pivot_longer and pivot_wider

## Frontmatter
- title: pivot_longer() and pivot_wider(): Reshape Data in R Without Losing Your Mind
- slug: pivot_longer-pivot_wider-Reshape-Data-in-R
- description: Reshape wide-to-long and long-to-wide in R with tidyr's pivot_longer() and pivot_wider(). Learn every key argument with before-and-after examples.
- keywords: pivot_longer, pivot_wider, tidyr, reshape data R, wide to long R, long to wide R, names_to, values_to, names_from, values_from
- auto_link_terms: pivot_longer()|pivot_wider()|pivot longer|pivot wider|reshape data in R|wide to long format|long to wide format
- post_type: C
- sidebar_section: Data Wrangling
- sidebar_title: pivot_longer & pivot_wider
- sidebar_order: 8

## Core sections (question form)
1. What does wide vs long data actually look like? (payoff: wide table + pivot_longer result)
2. How does pivot_longer() turn wide columns into rows?
3. How does pivot_wider() turn rows back into columns?
4. How do you split compound column names with names_sep and names_pattern?
5. What happens when pivot_wider() creates missing values?
6. How do you reshape multiple value columns at once?
7. When should you reshape vs keep data as-is?

## Diagrams (reuse existing)
1. pivot_longer-pivot_wider-wide-vs-long.webp — Fig 1: Wide vs long shape. In section 1.
2. pivot_longer-pivot_wider-longer-anatomy.webp — Fig 2: pivot_longer() argument anatomy. In section 2.
3. pivot_longer-pivot_wider-wider-anatomy.webp — Fig 3: pivot_wider() argument anatomy. In section 3.

## Tail
- Practice Exercises (3 capstones)
- Complete Example: messy survey → tidy summary
- Summary
- References
- Continue Learning
