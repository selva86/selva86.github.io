{
  "version": 1,
  "description": "Keep Pages Functions off pure asset paths: they need no auth, no KV, and no middleware, and they dominate request volume. Everything else (HTML pages, /api, extensionless twins, blocked source dirs) stays routed through functions/_middleware.ts. The middleware also carries an asset-extension short-circuit for anything this list misses.",
  "include": ["/*"],
  "exclude": [
    "/www/*",
    "/css/*",
    "/screenshots/*",
    "/figures/*",
    "/datasets/*",
    "/codes/*",
    "/favicon.ico",
    "/robots.txt",
    "/sitemap.xml",
    "/feed.xml"
  ]
}
