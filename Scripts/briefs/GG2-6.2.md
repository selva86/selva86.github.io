# Brief: GG2-6.2 Advanced Faceting in R (regenerate after manual_review)

Context: first write correctly made all ggh4x blocks static (package is not
browser-runnable) but that left only 5 live blocks vs the C-post minimum of 8.
Fix the structure, not the rule, and add no off-topic filler.

Restructure so the RUNNABLE SPINE is advanced faceting with base ggplot2,
which is fully on-topic and easily carries 8-12 live blocks:
- nested-look facets with interaction() + labeller(.multi_line), and
  label_wrap_gen for long strips
- facet_grid(space = "free", scales = "free") and when each matters
- strip placement: switch, strip.position, theme(strip.placement), styling
  strip.background/strip.text per side
- facet_wrap free scales per panel + ncol/nrow/dir layout control
- margins = TRUE totals panels
- reordering facet panels with factor levels / forcats
- per-panel highlighting: plotting the full data grey in every panel
  (data = transform(df, facetvar = NULL)) behind the per-panel layer

THEN the ggh4x tier (static blocks with real local-R outputs, clearly framed
as beyond what base faceting can do): facet_nested true nesting,
facetted_pos_scales per-panel axes, facet_grid2 independent scales.
Static blocks show code + #> output captured from local R; never fake output.

Keep title and slug unchanged: Advanced-Faceting-in-R. Everything else per
the standard v4 write rules (FAQ, no em dashes, no browser-runtime naming).
