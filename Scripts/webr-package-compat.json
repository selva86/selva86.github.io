{
  "note": "KNOWN-GOOD WebR package list. EVERY entry was PROVEN by actually running the package in real WebR (probe via the headless-WebR harness) - never guessed. Consult this BEFORE choosing a package for a lesson, so you pick a compatible one up front instead of finding out at verify-time. The per-lesson runner Scripts/verify_lesson_webr.mjs is the final ground truth for a lesson's actual code; this is the fast lookup the gate + /write-lesson use. To add a package: PROBE it once and record the evidence here. Statuses: runnable = runs and gives real output (live ```r block); runnable_no_visual = runs without error but its rich HTML/widget output does NOT render in WebR (use ```r for the data/text, show the visual as a static image, or ```r-static); illustrative = library loads but the lesson's real use needs a server / API key / network (use ```r-static); incompatible = errors in WebR (switch package or ```r-static).",
  "tested": "2026-06-25",
  "webr_repo_size": "~21,600 CRAN packages compiled to WASM at repo.r-wasm.org; most pure-R and many compiled packages work. Likely-bad (not yet needed, probe before use): GPU stacks (torch, tensorflow, keras), Java-backed (rJava and dependents), and anything needing a live system service. Servers/APIs load but cannot run their core use (see 'illustrative').",
  "base_always_runnable": ["base", "stats", "utils", "graphics", "grDevices", "datasets", "methods", "tools", "grid", "splines", "parallel"],
  "packages": {
    "dplyr": { "status": "runnable", "evidence": "filter/summarise run" },
    "tidyr": { "status": "runnable", "evidence": "pivot_longer runs" },
    "readr": { "status": "runnable", "evidence": "read_csv(I(...)) parses inline text" },
    "tibble": { "status": "runnable", "evidence": "tibble() runs" },
    "purrr": { "status": "runnable", "evidence": "map_dbl runs" },
    "stringr": { "status": "runnable", "evidence": "str_detect runs" },
    "forcats": { "status": "runnable", "evidence": "fct_rev runs" },
    "lubridate": { "status": "runnable", "evidence": "ymd parses dates" },
    "glue": { "status": "runnable", "evidence": "glue() interpolates" },
    "scales": { "status": "runnable", "evidence": "comma() formats" },
    "janitor": { "status": "runnable", "evidence": "clean_names runs" },
    "jsonlite": { "status": "runnable", "evidence": "toJSON runs" },
    "data.table": { "status": "runnable", "evidence": "RUNS single-threaded - call setDTthreads(1) first; dt[, .(m=mean(x)), by=g] works" },
    "dtplyr": { "status": "runnable", "evidence": "lazy_dt runs" },
    "ggplot2": { "status": "runnable", "evidence": "ggplot()+geom_point() builds AND print(p) renders headless" },
    "patchwork": { "status": "runnable", "evidence": "p1+p2 composes + prints" },
    "ggrepel": { "status": "runnable", "evidence": "geom_text_repel layers on ggplot" },
    "GGally": { "status": "runnable", "evidence": "ggpairs() builds + prints" },
    "skimr": { "status": "runnable", "evidence": "skim() returns a summary" },
    "DataExplorer": { "status": "runnable", "evidence": "introduce() runs" },
    "duckdb": { "status": "runnable", "evidence": "dbConnect(duckdb()) + dbGetQuery returns rows" },
    "DBI": { "status": "runnable", "evidence": "used with duckdb/RSQLite" },
    "randomForest": { "status": "runnable", "evidence": "randomForest(Species~., iris) trains - USE THIS, not ranger" },
    "rpart": { "status": "runnable", "evidence": "rpart() fits a tree" },
    "MASS": { "status": "runnable", "evidence": "lda() runs" },
    "broom": { "status": "runnable", "evidence": "tidy(lm(...)) runs" },
    "glmnet": { "status": "runnable", "evidence": "glmnet() fits" },
    "e1071": { "status": "runnable", "evidence": "svm() trains" },
    "caret": { "status": "runnable", "evidence": "train is a function; loads" },
    "lme4": { "status": "runnable", "evidence": "lmer() fits a mixed model" },
    "survival": { "status": "runnable", "evidence": "survfit/Surv run" },
    "forecast": { "status": "runnable", "evidence": "auto.arima + forecast run" },
    "zoo": { "status": "runnable", "evidence": "rollmean runs" },
    "knitr": { "status": "runnable", "evidence": "kable() returns a text table" },
    "gt": { "status": "runnable_no_visual", "evidence": "gt() builds a gt_tbl; styled HTML does not render in WebR - show print() or a static image" },
    "gtsummary": { "status": "runnable_no_visual", "evidence": "tbl_summary builds; gt/HTML output does not render in WebR" },
    "flextable": { "status": "runnable_no_visual", "evidence": "flextable builds; rich output does not render in WebR" },
    "kableExtra": { "status": "runnable_no_visual", "evidence": "kbl() builds; HTML styling does not render in WebR" },
    "plotly": { "status": "runnable_no_visual", "evidence": "plot_ly builds a plotly object; the interactive chart does not render in WebR - pair a static image or a built ggplot" },
    "leaflet": { "status": "runnable_no_visual", "evidence": "leaflet() builds; the map widget does not render in WebR" },
    "DT": { "status": "runnable_no_visual", "evidence": "datatable() builds; the interactive table does not render in WebR" },
    "crosstalk": { "status": "runnable_no_visual", "evidence": "SharedData runs; linked htmlwidget output does not render in WebR" },
    "shiny": { "status": "illustrative", "evidence": "library loads but an app cannot serve inside WebR - show app code as ```r-static" },
    "ellmer": { "status": "illustrative", "evidence": "library loads but a live chat needs an API key + network - show the call as ```r-static" },
    "ranger": { "status": "incompatible", "evidence": "ranger::ranger() ERRORS in WebR; use randomForest (runnable) instead" }
  }
}
